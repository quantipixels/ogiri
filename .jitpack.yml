jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.17-open
  - sdk use java 17.0.17-open

install:
  - ./gradlew clean build publishToMavenLocal

before_script:
  - echo "Building with Java 17"
  - java -version
