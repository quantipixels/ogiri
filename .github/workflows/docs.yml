name: Deploy Documentation

on:
  push:
    branches:
      - ori
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - ".github/workflows/docs.yml"
    tags:
      - "v*"
  release:
    types: [published]

permissions:
  contents: write

concurrency:
  group: docs
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e .

      - name: Configure git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Deploy versioned docs
        run: |
          if [ "${{ github.event_name }}" == "release" ]; then
            # Release event: deploy as versioned docs only
            VERSION=${{ github.event.release.tag_name }}
            VERSION=${VERSION#v}
            echo "Deploying release docs: $VERSION"
            mike deploy --push "$VERSION"
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            # Tag push: deploy as versioned docs only
            VERSION=${{ github.ref_name }}
            VERSION=${VERSION#v}
            echo "Deploying tagged docs: $VERSION"
            mike deploy --push "$VERSION"
          else
            # ori branch push: deploy as "latest"
            echo "Deploying ori branch docs as latest"
            mike deploy --push --update-aliases ori latest
            mike set-default --push latest
          fi

      - name: Verify deployment
        run: |
          echo "✓ Documentation deployed successfully"
          echo "Available at: https://mosobande.github.io/ogiri"
